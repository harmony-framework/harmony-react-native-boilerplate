import {RSValue} from '@utils/common';

const size = {
  S: RSValue(14),
  M: RSValue(16),
  L: RSValue(30),
};

const letterSpacing = {
  S: RSValue(2),
  M: RSValue(5),
  L: RSValue(10),
};

export const typography = {size, letterSpacing};
